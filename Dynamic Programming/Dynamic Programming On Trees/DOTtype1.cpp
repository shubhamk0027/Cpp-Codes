#include<bits/stdc++.h>
/**
 * Given a tree T of N nodes, where each node i has Ci coins attached with it. 
 * You have to choose a subset of nodes such that no 
 * two adjacent nodes(i.e. nodes connected directly by an edge) are chosen 
 * and sum of coins attached with nodes in chosen subset is maximum.
 */ 
using namespace std;

int main(){}